import { MisleadingRepresentationValidatorDirective } from './misleading-representation-validator.directive';

describe('MisleadingRepresentationValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MisleadingRepresentationValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
