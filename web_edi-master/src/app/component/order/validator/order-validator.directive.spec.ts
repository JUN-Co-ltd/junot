import { OrderValidatorDirective } from './order-validator.directive';

describe('OrderValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
