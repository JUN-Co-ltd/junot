import { PartNoInputPipe } from './part-no-input.pipe';

describe('PartNoInputPipe', () => {
  it('create an instance', () => {
    const pipe = new PartNoInputPipe();
    expect(pipe).toBeTruthy();
  });
});
