import { MaintNewsValidatorDirective } from './maint-news-validator.directive';

describe('MaintNewsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaintNewsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
