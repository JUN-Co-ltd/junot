import { ProductionStatusValidatorDirective } from './production-status-validator.directive';

describe('ProductionStatusValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductionStatusValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
