import { FabricInspectionValidatorDirective } from './fabric-inspection-validator.directive';

describe('FabricInspectionValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FabricInspectionValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
