import { Brand01ValidatorDirective } from './brand01-validator.directive';

describe('Brand01ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Brand01ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
