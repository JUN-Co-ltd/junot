import { CommonValidatorDirective } from './common-validator.directive';

describe('CommonValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
