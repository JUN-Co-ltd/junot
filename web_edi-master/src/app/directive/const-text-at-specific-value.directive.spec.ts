import { ConstTextAtSpecificValueDirective } from './const-text-at-specific-value.directive';

describe('ConstTextAtSpecificValueDirective', () => {
  it('should create an instance', () => {
    // const directive = new ConstTextAtSpecificValueDirective();
    // expect(directive).toBeTruthy();
  });
});
