import { Brand03ValidatorDirective } from './brand03-validator.directive';

describe('Brand03ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Brand03ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
