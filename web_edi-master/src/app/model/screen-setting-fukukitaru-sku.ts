/**
 * フクキタル連携 画面用SKU情報のModel.
 */
export class ScreenSettingFukukitaruSku {
    /** カラーコード. */
    colorCode: string;
    /** カラーコード名. */
    colorName: string;
    /** サイズ. */
    size: string;
    /** 活性(true)/非活性(false). */
    enabled: boolean;
}
