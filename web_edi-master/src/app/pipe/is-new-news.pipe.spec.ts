import { IsNewNewsPipe } from './is-new-news.pipe';

describe('IsNewNewsPipe', () => {
  it('create an instance', () => {
    const pipe = new IsNewNewsPipe();
    expect(pipe).toBeTruthy();
  });
});
