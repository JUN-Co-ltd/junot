import { PartNoInputDirective } from './part-no-input.directive';

describe('PartNoInputDirective', () => {
  it('should create an instance', () => {
    // const directive = new PartNoInputDirective();
    // expect(directive).toBeTruthy();
  });
});
