import { DeriveryStoreValidatorDirective } from './derivery-store-validator.directive';

describe('DeriveryStoreValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DeriveryStoreValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
