import { FromToCheckDirective } from './from-to-check.directive';

describe('FromToCheckDirective', () => {
  it('should create an instance', () => {
    // const directive = new FromToCheckDirective();
    // expect(directive).toBeTruthy();
  });
});
