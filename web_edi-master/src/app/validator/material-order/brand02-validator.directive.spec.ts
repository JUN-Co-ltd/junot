import { Brand02ValidatorDirective } from './brand02-validator.directive';

describe('Brand02ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Brand02ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
