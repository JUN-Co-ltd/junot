import { ArrayRequiredDirective } from './array-required.directive';

describe('ArrayRequiredDirective', () => {
  it('should create an instance', () => {
    // const directive = new ArrayRequiredDirective();
    // expect(directive).toBeTruthy();
  });
});
