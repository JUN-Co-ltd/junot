import { IsWithinOpenPeriodPipe } from './is-within-open-period.pipe';

describe('IsWithinOpenPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new IsWithinOpenPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
