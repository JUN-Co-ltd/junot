import { MakerReturnStatusPipe } from './maker-return-status.pipe';

describe('MakerReturnStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MakerReturnStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
