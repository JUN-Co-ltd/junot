import { DeliveryCountPipe } from './delivery-count.pipe';

describe('DeliveryCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryCountPipe();
    expect(pipe).toBeTruthy();
  });
});
