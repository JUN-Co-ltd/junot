import { FromToCheck3Directive } from './from-to-check3.directive';

describe('FromToCheck3Directive', () => {
  it('should create an instance', () => {
    // const directive = new FromToCheck3Directive();
    // expect(directive).toBeTruthy();
  });
});
