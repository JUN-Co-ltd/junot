import { DeliveryRequestValidatorDirective } from './delivery-request-validator.directive';

describe('DeliveryRequestValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DeliveryRequestValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
