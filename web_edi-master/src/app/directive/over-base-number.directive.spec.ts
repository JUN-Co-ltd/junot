import { OverBaseNumberDirective } from './over-base-number.directive';

describe('OverBaseNumberDirective', () => {
  it('should create an instance', () => {
    // const directive = new OverBaseNumberDirective();
    // expect(directive).toBeTruthy();
  });
});
