import { RequiredRelation2Directive } from './required-relation2.directive';

describe('RequiredRelation2Directive', () => {
  it('should create an instance', () => {
    const directive = new RequiredRelation2Directive();
    expect(directive).toBeTruthy();
  });
});
