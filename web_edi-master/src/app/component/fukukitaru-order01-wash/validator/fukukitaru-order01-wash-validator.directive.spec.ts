import { FukukitaruOrder01WashValidatorDirective } from './fukukitaru-order01-wash-validator.directive';

describe('FukukitaruOrder01WashValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FukukitaruOrder01WashValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
