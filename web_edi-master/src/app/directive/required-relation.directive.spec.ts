import { RequiredRelationDirective } from './required-relation.directive';

describe('RequiredRelationDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredRelationDirective();
    expect(directive).toBeTruthy();
  });
});
