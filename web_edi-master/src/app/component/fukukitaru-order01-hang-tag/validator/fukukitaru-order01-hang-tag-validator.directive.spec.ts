import { FukukitaruOrder01HangTagValidatorDirective } from './fukukitaru-order01-hang-tag-validator.directive';

describe('FukukitaruOrder01HangTagValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FukukitaruOrder01HangTagValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
