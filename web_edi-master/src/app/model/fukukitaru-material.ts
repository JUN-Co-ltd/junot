/**
 * フクキタル連携 資材情報のModel.
 */
export class FukukitaruMaterialModel {
  /** ID. */
  id: number;
  /** コード. */
  code: string;
  /** コード名称. */
  codeName: string;
  /** 出荷単位. */
  moq: number;
  /** 資材種別. */
  materialType: number;
  /** 資材種別ネーム. */
  materialTypeName: number;
}
