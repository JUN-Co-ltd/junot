import { DeliveryPlanValidatorDirective } from './delivery-plan-validator.directive';

describe('DeliveryPlanValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DeliveryPlanValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
