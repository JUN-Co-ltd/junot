import { DeliveryDistributionModalValidatorDirective } from './delivery-distribution-modal-validator.directive';

describe('DeliveryDistributionModalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DeliveryDistributionModalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
