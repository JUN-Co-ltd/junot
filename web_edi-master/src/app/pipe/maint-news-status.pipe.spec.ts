import { MaintNewsStatusPipe } from './maint-news-status.pipe';

describe('MaintNewsStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MaintNewsStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
