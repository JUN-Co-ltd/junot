import { InstructorSystemTypePipe } from './instructor-system-type.pipe';

describe('InstructorSystemTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InstructorSystemTypePipe();
    expect(pipe).toBeTruthy();
  });
});
